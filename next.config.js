/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    images: { 
        domains: [
            'links.papareact.com',
            'plus.unsplash.com',
            'images.unsplash.com',
            // Add more domains here if needed
        ],
    }
}
